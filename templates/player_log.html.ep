% use TileSvg qw(xy_coords);
<h4>Log:</h4>
<div>
<table>
<tbody>
<% my $it = $session->log_iterator($p_id); while ( my $log = $it->() ) { %>\
<tr coords="<%= join ',' => xy_coords( $log->view_rn ) %>" onclick='handleLogClick.call(this)'>
<td><%= $log->player->name %></td>
<td>→</td>
<td><%= $log->tile->name %></td>
</tr>
<% } %>\
</tbody>
</table>
</div>
